import { describe, expect, it } from "vitest";

// Placeholder test for Etapa 0 (setup do projeto) — garante que `npm test` roda
// antes de existir lógica real para testar. Remover quando a Etapa 1 (parsing
// de charts) trouxer os primeiros testes de verdade.
describe("etapa 0 - setup", () => {
  it("roda o test runner", () => {
    expect(true).toBe(true);
  });
});
